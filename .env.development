# Loaded by Vite (`npm run dev`) in addition to `.env`.
# Jitsi connection comes from `.env` (VITE_JITSI_PUBLIC_URL → http://localhost:8001).

# Optional HTTPS for local Vite (requires ./.cert/cert.pem and key.pem):
# HTTPS=true
# SSL_CRT_FILE=./.cert/cert.pem
# SSL_KEY_FILE=./.cert/key.pem

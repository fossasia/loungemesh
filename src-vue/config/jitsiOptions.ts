export const conferenceNameDefault = 'flowspace';

export const conferenceOptions = {
  /** Requires a working /colibri-ws proxy; leave false if bridge WS fails (1006). */
  openBridgeChannel: false,
  channelLastN: 16,
  enableLayerSuspension: false,
};

# Flowspace (Vue SPA) + local Jitsi Meet (docker-jitsi-meet stack).
#
#   docker compose up -d --build
#
# Flowspace UI: http://localhost:8780
# Jitsi web:    http://localhost:8001  (PUBLIC_URL in .env)
#
# Image versions are pinned here so Dependabot can open update PRs automatically.
# Do NOT use ${JITSI_IMAGE_VERSION} or floating tags like :stable — pin explicitly.

services:
  init:
    image: busybox:1.37.0
    volumes:
      - .:/work
    working_dir: /work
    command:
      - sh
      - -c
      - |
        set -e
        mkdir -p docker/jitsi-config/web/crontabs
        mkdir -p docker/jitsi-config/transcripts
        mkdir -p docker/jitsi-config/prosody/config
        mkdir -p docker/jitsi-config/prosody/prosody-plugins-custom
        mkdir -p docker/jitsi-config/jicofo
        mkdir -p docker/jitsi-config/jvb
    restart: "no"

  flowspace:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_JITSI_PUBLIC_URL: ${PUBLIC_URL:-http://localhost:8001}
        VITE_XMPP_DOMAIN: ${XMPP_DOMAIN:-meet.jitsi}
        VITE_XMPP_MUC_DOMAIN: ${XMPP_MUC_DOMAIN:-muc.meet.jitsi}
        VITE_USE_WEBSOCKET: "true"
        VITE_EVENTYAY_API_BASE: ${VITE_EVENTYAY_API_BASE-}
        VITE_SESSION_PREFIX: ${VITE_SESSION_PREFIX-}
    ports:
      - "${FLOWSPACE_PORT:-8780}:80"
    depends_on:
      init:
        condition: service_completed_successfully
      jitsi-web:
        condition: service_started
    restart: unless-stopped

  jitsi-web:
    image: jitsi/web:stable-9953
    restart: ${RESTART_POLICY:-unless-stopped}
    ports:
      - "${HTTP_PORT:-8001}:80"
      - "${HTTPS_PORT:-8444}:443"
    volumes:
      - ${CONFIG:-./docker/jitsi-config}/web:/config:Z
      - ${CONFIG:-./docker/jitsi-config}/web/crontabs:/var/spool/cron/crontabs:Z
      - ${CONFIG:-./docker/jitsi-config}/transcripts:/usr/share/jitsi-meet/transcripts:Z
    env_file:
      - .env
    depends_on:
      init:
        condition: service_completed_successfully
      jvb:
        condition: service_started
    networks:
      meet.jitsi:

  prosody:
    image: jitsi/prosody:stable-9953
    restart: ${RESTART_POLICY:-unless-stopped}
    expose:
      - "5222"
      - "5269"
      - "5347"
      - "5280"
    volumes:
      - ${CONFIG:-./docker/jitsi-config}/prosody/config:/config:Z
      - ${CONFIG:-./docker/jitsi-config}/prosody/prosody-plugins-custom:/prosody-plugins-custom:Z
    env_file:
      - .env
    depends_on:
      init:
        condition: service_completed_successfully
    networks:
      meet.jitsi:
        aliases:
          - ${XMPP_SERVER:-xmpp.meet.jitsi}

  jicofo:
    image: jitsi/jicofo:stable-9953
    restart: ${RESTART_POLICY:-unless-stopped}
    ports:
      - "127.0.0.1:${JICOFO_REST_PORT:-8888}:8888"
    volumes:
      - ${CONFIG:-./docker/jitsi-config}/jicofo:/config:Z
    env_file:
      - .env
    depends_on:
      init:
        condition: service_completed_successfully
      prosody:
        condition: service_started
    networks:
      meet.jitsi:

  jvb:
    image: jitsi/jvb:stable-9953
    restart: ${RESTART_POLICY:-unless-stopped}
    ports:
      - "${JVB_PORT:-10000}:${JVB_PORT:-10000}/udp"
      - "127.0.0.1:${JVB_COLIBRI_PORT:-8090}:8080"
    volumes:
      - ${CONFIG:-./docker/jitsi-config}/jvb:/config:Z
    env_file:
      - .env
    depends_on:
      init:
        condition: service_completed_successfully
      prosody:
        condition: service_started
    networks:
      meet.jitsi:

networks:
  meet.jitsi:
